# 导入path函数from django.urls import path# 导入此目录下views模块from . import views# 添加app的名称用来设置app在根目录下urls.py的命名空间,2.0版本不在根目录添加namespace也能使用,可选app_name = 'polls'# 添加url名称,设置对应的views函数,并且可以命名该url的名称urlpatterns = [    path('', views.index, name='index'),    # <>内表示捕捉部分url然后作为关键字参数传送给views函数    path('<int:question_id>/', views.detail, name='detail'),    # int是一个转换器,用来确定什么模式应该匹配这部分url    path('<int:question_id>/results/', views.results, name='detail'),    # question_id定义了用来标识匹配模式的名称,将匹配到的模式取一个名称    path('<int:question_id>/vote/', views.vote, name='vote')]